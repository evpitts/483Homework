% TODO: finish parameters file
boxParamHWA % Load parameters

% Generate the reference signal
reference = signalGenerator(0.5, 0.1);

% Plot the box
figure
rectangle('Position', [0 0 2 1], )
%dataPlot = plotData(B);